generates a public data stream from the Hacker News RSS feed

```
pip install pubnub
pip install feedparser
python hn.py --time 3
```

then open build/index.html

the live demo view is in build/iframe.html
